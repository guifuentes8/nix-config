{ pkgs, ... }: {
  stylix = {
    base16Scheme = "${pkgs.base16-schemes}/share/themes/tokyo-night-dark.yaml";
    cursor = { };
    fonts = { };
    image = ./wallpaper.jpg;
    opacity = { };
    override = { };
    targets = { };
  };
}

